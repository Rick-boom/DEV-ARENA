/**
 * Public entry point of @devarena/shared-types.
 * Only foundation-level contracts live here for now; feature types
 * (battles, rooms, submissions, ...) are added in their own modules
 * as those features are designed.
 */
export * from './api.js';
export * from './enums.js';
