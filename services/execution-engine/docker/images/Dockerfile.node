# JavaScript sandbox image. Official Node 22 slim. Interpreted; the
# runner passes --max-old-space-size to align the V8 heap with the
# container memory limit.
FROM node:22-slim

WORKDIR /sandbox
USER 65534:65534
CMD ["true"]
