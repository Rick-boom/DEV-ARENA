// C++17 starter template. Reads all of stdin; the judge feeds test
// input on stdin and compares stdout. Replace the body with a solution.
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    // Example: echo two summed integers.
    long long a, b;
    if (cin >> a >> b) cout << (a + b) << "\n";
    return 0;
}
